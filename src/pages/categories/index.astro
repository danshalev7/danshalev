---
import Layout from "@/layouts/Layout.astro";
import { getPosts } from "@/utils/post";
import { Icon } from "astro-icon/components";
import { getCollection } from "astro:content";

const allCategories = await getCollection("categories");
const allPosts = await getPosts();

const categoryCounts = allPosts.reduce((acc: Record<string, number>, post) => {
  const catId = post.data.category.id;
  acc[catId] = (acc[catId] || 0) + 1;
  return acc;
}, {});
---

<Layout title="Categories" description="Browse all categories.">
  <div
    class="animate-in fade-in slide-in-from-bottom-4 mx-auto max-w-2xl p-8 duration-700 md:p-16"
  >
    <header class="mb-16">
      <h2
        class="mb-4 text-3xl font-black tracking-tighter text-on-surface uppercase"
      >
        Categories
      </h2>
      <p class="mb-12 text-sm font-light text-on-surface-muted italic">
        {allCategories.length} categories in total.
      </p>
    </header>

    <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
      {
        allCategories.map((cat) => (
          <a
            href={`/categories/${cat.data.slug}`}
            class="group flex items-center gap-4 rounded-2xl bg-surface-low p-5 transition-all duration-100 ease-snap hover:-translate-y-1"
          >
            <div class="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-flat-cobalt/15 text-flat-cobalt transition-all duration-100 ease-snap group-hover:scale-110">
              <Icon name={cat.data.icon} class="h-6 w-6" />
            </div>
            <div class="min-w-0 flex-1">
              <div class="mb-1 flex items-center gap-2">
                <h3 class="truncate text-base font-bold group-hover:text-flat-cobalt">
                  {cat.data.name}
                </h3>
                <span class="shrink-0 rounded bg-flat-cobalt/10 px-1.5 py-0.5 font-mono text-[10px] text-flat-cobalt/70">
                  {categoryCounts[cat.id] || 0}
                </span>
              </div>
              <p class="line-clamp-2 text-xs text-on-surface-muted">
                {cat.data.description || "No description"}
              </p>
            </div>
          </a>
        ))
      }
    </div>
  </div>
</Layout>
